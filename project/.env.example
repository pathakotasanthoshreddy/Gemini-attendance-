# Add .env file
echo 'VITE_API_URL=https://s4foru.in/api' > .env

# Rebuild frontend
npm run build

# Deploy build to Nginx web root
sudo cp -r dist/* /var/www/s4foru.in/
sudo nginx -t && sudo systemctl reload nginx

